# Mandala Board

PCブラウザ上で 9×9 のマンダラートを直接編集できる静的Webページです。各セルはテキストエリアになっており、入力内容はブラウザの `localStorage` に自動保存されます。

## 使い方

- `index.html` をブラウザで開きます。
- 各セルをクリックして直接入力します。
- Enter または矢印キーでセル間を移動できます。
- 「全消去」で内容をクリアできます。
- 「サンプルを挿入」で初期の記入例を入れられます。

## 補足

- ビルド依存はありません。
- オフラインでも動作します。
